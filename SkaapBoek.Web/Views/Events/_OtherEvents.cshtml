@*@model OtherEventsModel

<div class="text-light">
  <div class="row">

    <div class="col-12 col-lg-6 col-xl-4">
      <div class="card event-card-main mt-4">
        <div class="card-header sticky-taskheader">
          <h5 class="card-title">Sheep</h5>
          <h6 class="card-subtitle text-white-50">Other sheep related events.</h6>
        </div>
        <div class="card-body p-3">
          <div id="tasks-today">
            @foreach (var item in Model.SheepEvents)
            {
              <div class="card bg-dark mb-3 card-inner border">
                <div class="card-body p-3">

                  <dl class="row p-0 m-0">
                    <dt class="col-4 p-0">Tag:</dt>
                    <dd class="col-8 p-0">@item.Sheep.TagNumber</dd>
                    <dt class="col-4 p-0">Color:</dt>
                    <dd class="col-8 p-0">@item.Sheep.Color.Name</dd>
                    <dt class="col-4 p-0">Pen:</dt>
                    <dd class="col-8 p-0">@item.Sheep.Pen.Name</dd>
                    <dt
                  </dl>

                  <div class="d-flex justify-content-between">
                    <a tabindex="0" class="btn btn-sm btn-outline-secondary text-light mils-info-popover"
                       role="button" data-toggle="popover" data-trigger="focus" title="Additional Info"
                       data-placement="bottom">More info</a>

                    @if (item.EndDate.Date == DateTime.Today)
                    {
                      <button class="btn btn-primary btn-sm" data-toggle="modal"
                              data-target="#moveToPhaseModal">
                        Move to phase
                      </button>
                    }
                  </div>
                </div>
              </div>
            }
            <div class="alert alert-info m-0"
                 hidden="@Model.TodaysMilsGroups.Any()">
              No phase events today.
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>*@
